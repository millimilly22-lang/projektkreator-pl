# ProjektKreator.pl

Fresh React + Vite rebuild based on the approved ProjektKreator HTML reference. The application contains Usługi, Nasze realizacje, Cennik and Napisz do nas as real React UI.

Build: `npm install && npm run build`

Render publish directory: `dist`.
