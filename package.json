{
  "name": "projektkreator-pl",
  "version": "2.0.2",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/pages && cp index.html style.css script.js dist/ && cp pages/*.html dist/pages/ && echo 'Static site copied to dist'",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
