services:
  - type: web
    name: projektkreator-pl-web
    runtime: node
    buildCommand: rm -rf dist node_modules/.vite && npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 22
